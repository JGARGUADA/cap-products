{
  "name": "products",
  "version": "1.0.0",
  "description": "Service Odata V4 - Products",
  "repository": "https://github.com/JGARGUADA/cap-products.git",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8.6.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.7.0",
    "@cap-js/sqlite": "^1.7.8"
  },
  "cds": {
    "requires": {
      "[dev]": {
        "db": {
          "kind": "sqlite",
          "credentials": {
            "database": "db.sqlite"
          }
        },
        "auth":"dummy"
      }
    }
  },
  "scripts": {
    "start": "cds-serve",
    "start-dev": "cds-serve --profile dev"
  }
}
